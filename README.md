# Linked In company id scraper

set up the linkedin mail and password at the .env file 

then run the command 

First clone the repository
```
cd LinkedIn-Scraper
cd app
pip install -r requirements.txt
```
ensure mangodb in your server 
run the below command
```
uvicorn main:app --reload
```
Wherre post the postman.json into postman to verify about endpoints .
